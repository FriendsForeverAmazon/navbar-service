config:
  target: "http://localhost:3003/products/all departments"
  phases:
    - duration: 60
      arrivalRate: 100
  processor: "./generateData.js"
scenarios:
  - flow:
    - function: "generateRandomData"
    - get:
        url: "/{{ query }}"
