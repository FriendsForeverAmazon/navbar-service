config:
  target: "http://localhost:3003/products/create"
  phases:
    - duration: 60
      arrivalRate: 100
  processor: "./generateData.js"
scenarios:
  - flow:
    - function: "generateRandomData"
    - post:
        url: "/{{ query }}"
